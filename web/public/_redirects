# Invite links are real URLs (/m/KX7PQ), but there's no such file — the client reads the code out
# of the path. Static hosts need telling to answer them with the page itself. Cloudflare Pages and
# Netlify both read this file; Render ignores it and does the same thing in server.js.
/m/*  /index.html  200
