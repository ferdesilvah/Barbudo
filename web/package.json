{
  "name": "barbudo-web",
  "version": "0.3.0",
  "private": true,
  "description": "Barbudo — the family card game, multiplayer in the browser",
  "type": "module",
  "main": "server.js",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
